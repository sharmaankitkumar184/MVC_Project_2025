@model MVC_Project.Models.Models.Department

@{
    ViewData["Title"] = "Create Department";
}

<div class="form-header">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h1><i class="bi bi-building-add me-3"></i>Create New Department</h1>
            <p>Add a new department to your organization</p>
        </div>
    </div>
</div>

<div class="form-card">
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <!-- Validation Summary -->
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="validation-alert alert-danger">
                    <strong><i class="bi bi-exclamation-circle me-2"></i>Validation Error</strong>
                    @Html.ValidationSummary(false)
                </div>
            }

            <!-- Department Information Section -->
            <div class="form-section">
                <div class="form-section-title">
                    <i class="bi bi-building" style="color: #667eea;"></i>
                    Department Information
                </div>

                <!-- Department Name -->
                <div class="form-group">
                    <label asp-for="Name"><i class="bi bi-tag me-2"></i>Department Name</label>
                    <input asp-for="Name" class="form-control" placeholder="e.g., Sales, Marketing, HR, IT" required />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <!-- Description -->
                <div class="form-group">
                    <label asp-for="Description"><i class="bi bi-file-text me-2"></i>Description</label>
                    <textarea asp-for="Description" class="form-control" rows="4" placeholder="Enter department description (optional)"></textarea>
                    <small class="text-muted">Provide details about the department's purpose and responsibilities</small>
                </div>

                <!-- Status -->
                <div class="form-group">
                    <div class="form-check form-switch">
                        <input asp-for="IsActive" class="form-check-input" type="checkbox" checked />
                        <label asp-for="IsActive" class="form-check-label"><i class="bi bi-toggle2-on me-2"></i>Active Department</label>
                    </div>
                    <small class="text-muted d-block mt-2">Check this box if the department is currently active</small>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="button-group">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-circle me-2"></i>Create Department
                </button>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-x-circle me-2"></i>Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
